package pong

const (
	// window
	windowWidth  = 800
	windowHeight = 600

	// speed
	initialBallSpeed   = 5.0
	initialPaddleSpeed = 10.0
	speedUpdateCount   = 6
	speedIncrement     = 0.5

	// sizes
	ballRadius   = 10.0
	paddleWidth  = 20
	paddleHeight = 100
	paddleShift  = 50
)
